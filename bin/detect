#!/usr/bin/env bash
#
# Usage:
#
# ./bin/detect BUILD_DIR

BUILD_DIR=$1
EXPECTED_PATH=$BUILD_DIR/vendor/chromedriver

if [ -d $EXPECTED_PATH ]; then
  echo "Not installed." && exit 1
else
  echo "Installed." && exit 0
fi
